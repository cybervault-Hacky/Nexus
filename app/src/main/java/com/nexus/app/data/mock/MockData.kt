package com.nexus.app.data.mock

/**
 * Mock data for Phase 1 UI components.
 *
 * Context data was removed in Phase 2 (Room-backed).
 * Capsule data was removed in Phase 5 (Room-backed).
 * This object remains for any remaining Phase 1 UI dependencies
 * (e.g. QuickAction icons) and will be fully removed when
 * all remaining mock data is replaced.
 */
object MockData
